# Before applying, replace tenant-workshopXX with your namespace.
# Easier to do it across all files at once: see README, the section on substituting the number.
# Also: in the url field below, insert the presigned link from convert.sh (step 2).
#
apiVersion: apps.cozystack.io/v1alpha1
kind: VMDisk
metadata:
  name: app-1
  namespace: tenant-workshopXX
spec:
  source:
    http:
      url: "ВСТАВЬТЕ_PRESIGNED_URL"  # from convert.sh (Share: line)
  storage: 10Gi
  storageClass: replicated
---
apiVersion: apps.cozystack.io/v1alpha1
kind: VMInstance
metadata:
  name: app-1
  namespace: tenant-workshopXX
spec:
  instanceType: u1.medium
  instanceProfile: centos.7
  runStrategy: Always
  disks:
    - name: app-1
# External access to the app is via `virtctl port-forward` (see README).
# external LoadBalancer on this stand (Cilium + robotlb) does not work for VMs,
# so we do NOT enable external here.