## 3. 开始之前：你需要准备什么

**做好准备**

我们将从你的笔记本上管理集群。你需要四个工具——现在就把它们装好，因为从这里往后的每一步都会用到它们。

• `kubectl` —— 主力工具。我们用它来创建虚拟机、磁盘和数据库。
• `virtctl` —— 一切和虚拟机相关的操作：console、SSH、port-forward。
• `kubelogin` —— 通过浏览器用你自己的账号登录。
• `git` —— 我们用它来拉取那个装着工作坊现成文件的目录。

接下来的几条消息里，是针对各操作系统的命令。
装好之后，如果有什么没装上，就在群里说一声。
