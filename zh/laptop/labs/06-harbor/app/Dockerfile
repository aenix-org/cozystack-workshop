# 实验 6 · 你自己的私有镜像仓库。«Пропуск» 服务镜像的构建配方。
#
# 什么是 Dockerfile：一份把源码变成镜像的分步配方——
# 一个随时可运行的文件系统快照，连同在其中运行什么的指令一起打包。
# 在 vSphere 里最接近的类比是构建黄金 VM 模板，只不过这里用文本描述且
# 可重复：同一份配方在任何机器上都产出同样的结果。
#
# 在你的笔记本上、从实验目录构建（最后一个参数是包含此文件和源码的目录）：
#     docker build --platform linux/amd64 -t HARBOR-HOST/passes/passes-api:v1 app/
#     docker push HARBOR-HOST/passes/passes-api:v1
# 把 HARBOR-HOST 替换成你自己的 Harbor 地址：镜像名的第一部分就是仓库地址，
# docker push 正是据此知道该把镜像推送到哪里。
#
# 注意：在配备 Apple Silicon 处理器的 Mac 上以及任何 ARM 笔记本上，--platform linux/amd64 是必需的。
# 没有它你会得到一个 ARM 镜像：它能无错误地构建、无错误地推送到仓库，
# 然后在集群里（其节点是 x86）崩溃并报 «exec format error»。
#
# 配方的每一行都会创建一个层——文件系统的一个不可变切片。层会被
# 缓存和复用：这就是为什么文件是一个一个地复制而不是整个目录
# 一次性复制——修改 main.go 不会强制重新下载依赖。
#
# 两个阶段。第一个是构建阶段：它需要整个 Go 编译器，约 350 MB。
# 第二个是要发布到集群的部分：只有编译好的二进制被搬运到那里。
# 最终镜像约 10 MB 而非 350 MB，且其中既没有编译器，
# 也没有源码，也没有包管理器——也就是说，即便有人真的进到容器里，也无从下手。

# --- 阶段 1：构建 -----------------------------------------------------------------
# FROM——我们从哪个现成镜像开始。这里是 Alpine（一个极简 Linux，只有几兆字节）之上
# 带有 Go 编译器的官方镜像。版本被固定：若用 latest 标签，同一份配方半年后会构建出不同的东西。
# AS build——阶段的名字；第二阶段将据此取回结果。
FROM golang:1.23-alpine AS build
# 镜像内的工作目录：后续命令相对于它执行。
# 如果目录不存在，就会被创建。
WORKDIR /src
# go.mod——模块描述：它的名字和 Go 版本。应用没有外部依赖，
# 所以没有东西要从互联网下载，构建在没有对外访问的环境里也能进行。
COPY go.mod ./
# 源码本身。单独一行复制，这样修改代码不会使上一层的缓存失效。
COPY main.go ./
# RUN 在构建时（而非运行时）执行命令，并把结果保存为一个层。
#   CGO_ENABLED=0  静态构建，不绑定系统库。没有它
#                  二进制会需要 glibc，而最终镜像里不会有它，
#                  容器会在启动时崩溃而不是在构建时
#   -trimpath      从二进制里剥除构建机器的路径：构建变得
#                  可重现，而且你家目录的名字不会外泄
#   -ldflags="-s -w"  丢弃调试表——文件会明显变小
#   -o /out/...    把结果放到哪里；第二阶段从这里取走它
RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /out/passes-api .

# --- 阶段 2：要发布到集群的部分 -----------------------------------------------
# 第二个 FROM 从一张白纸重新开始镜像：第一阶段积累的一切
# 都留在那里，不会进入最终镜像。
FROM alpine:3.21
# 创建应用将以之运行的用户：它绝不能以 root 运行。
# 在配置正确的集群里，以 root 运行的 pod 根本不会被准入，这不是
# 吹毛求疵，而是一项标准的安全要求。
#   -D          不设密码、不问任何问题地创建
#   -u 10001    数字标识符；集群检查的正是这个数字，而不是名字
RUN adduser -D -u 10001 app
# 从第一阶段搬运过来的唯一东西，就是编译好的二进制。--from=build
# 指向阶段名。/usr/local/bin 是任何 Linux 的 PATH 里都存在的目录。
COPY --from=build /out/passes-api /usr/local/bin/passes-api
# 从这里开始，容器里的进程以用户 10001 运行，而不是 root。
# 顺序很重要：上面几行是以 root 执行的，否则 adduser 不会成功。
USER 10001
# 声明端口——给人和工具看的一条信息性说明。它并不打开端口，
# 也不配置任何东西：对它的访问在清单（passes.yaml）里描述。
EXPOSE 8080
# 容器启动时运行什么。用列表形式而非字符串：这样命令
# 直接运行，没有中间的 shell，来自集群的停止信号
# 会到达应用本身，而不是产生它的那个 shell。
ENTRYPOINT ["/usr/local/bin/passes-api"]
